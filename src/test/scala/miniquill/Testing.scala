package miniquill

object Testing {
  // test of quoting a literal
  // test of quoting a variable
  // test of quoting a that depends on a runtime-quote

  // testing a quote that depends on a compile-quote in a compile-quote
  // testing a quote that depends on a compile-quote in a compile-quote with lifted values
  // testing a quote that depends on a compile-quote(lift(somevalue)) union compile-quote(lift(somevalue)) --same value lifted twice i.e. has same uuid

  // testing a quote that depends on a runtime-quote in a runtime-quote

  // TODO See what tests there are of quill quotations and see if they can be ported
  
}