package io.getquill

// compose the standard quote with an additional parser for 'like'

// composition of a lifter/unlifter with a new datatype (maybe call it a "transparent-container"
// which is treated as a case class by the transformations. Might need some enchancements to AST though)